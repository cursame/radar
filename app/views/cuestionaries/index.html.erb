<h1>Cuestionarios</h1>
<%= form_tag '/cuestionaries/filter_repsonces' do -%>
   <%= select_tag 'instituciones', options_from_collection_for_select(@institutions, "id", "name")%>
   <% @i = current_user.institutions.last%>
   <%#= hidden_field_tag 'instituciones', @i.id%>
   <%= select_tag 'cuestionarios', options_from_collection_for_select(@questionaries, "id", "title")%>

   <%= submit_tag "Filtrar"%>
<%end%>
<%= link_to 'Nuevo curestionario',new_cuestionary_path%>
